import { TCheckoutbox, TDate, TEmail, TFile, TImage, TNumber, TPassword, TSelect, TTel, TText, TUrl } from './elements';

export type TElement = Array<TCheckoutbox | TDate | TEmail | TFile | TImage | TNumber | TPassword | TSelect | TTel | TText | TUrl>;
